#include <iostream>
#include <bits/stdc++.h>
#define int long long int
using namespace std;

int32_t main()
{
    int n, m, i, j, t;
    cin >> t;
    while (t--)
    {
        cin >> n >> m >> i >> j;
        cout << n << " " << m << " " << 1 << " " << 1 << endl;
    }
    return 0;
}